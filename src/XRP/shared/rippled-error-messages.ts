/**
 * An enum for creating constants that represent possible error messages received from the rippled WebSocket API.
 */
export enum RippledErrorMessages {
  accountNotFound = 'actNotFound',
  invalidExcludedAddress = 'invalidHotWallet',
}
