coverage:
  status:
    patch:
      default:
        target: 70%
        threshold: .01
        base: auto
        if_ci_failed: error
    project:
      default:
        target: 70%
        threshold: 0.01
        base: auto
        if_ci_failed: error
